OPENAI_API_KEY=your-api-key
# 留空或删除该行即可使用 OpenAI 默认地址
OPENAI_BASE_URL=
MODEL=gpt-4.1-mini

# Web/API 可选本地存储位置；不设置时使用 checkpoint_project 下的默认目录
# CHECKPOINT_DB=./checkpoint_project/data/checkpoints.sqlite
# CHECKPOINT_WORKSPACE=./checkpoint_project/workspace
