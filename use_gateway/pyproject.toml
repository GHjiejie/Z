[project]
name = "use-gateway"
version = "0.1.0"
description = "A runnable LiteLLM AI Gateway demo"
requires-python = ">=3.13"
dependencies = [
    "litellm[proxy]>=1.74.0,<2",
    "openai>=3.0.0,<4",
    "python-dotenv>=1.0.1,<2",
]

[tool.uv]
package = false
