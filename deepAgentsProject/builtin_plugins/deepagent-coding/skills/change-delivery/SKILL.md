# Change Delivery

- Review the platform-computed diff rather than relying on memory.
- Ensure the patch contains only intended changes and no credentials or generated clutter.
- Report file additions, modifications, deletions, and meaningful behavior changes.
- Include verification commands and outcomes in the final response.
- Default delivery is a reviewable patch. A patch is not a commit, push, PR, merge, or deployment.
